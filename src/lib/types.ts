export type TInquiryContact = {
  name: string;
  email: string;
  brand: string;
  services: string;
  budget: string;
  message: string;
};

export type TPricingGuide = {
  name: string;
  email: string;
};

export type TNewsletter = {
  email: string;
};
